/* jsx--------- */
{
    /* 
<DebounceBtn
    onActive={webStore.saveData}
    onActiveParams={SAVE_TYPE.DRAFT}
    styleClass={styles.load_btn}
    onActiveSuccess={() => {
        history.push('/gateway/web/list')
    }}
>
    <Button type={'default'} className={styles.draft_btn}>
        保存草稿
    </Button>
</DebounceBtn> 
*/
}

/* hooks--------- */
// const saveData = (status: SAVE_TYPE) => {
//     const requestParams = {
//         status,
//     }
//     return http(api.url, 'POST', requestParams)
//         .then(res => {
//             return Promise.resolve(res)
//         })
//         .catch(err => {
//             return Promise.reject(err)
//         })
// }
